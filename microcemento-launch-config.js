// Presentation only. Bold pricing and confirmed-order quotas remain server responsibilities.
window.KaementoLaunchCampaign = Object.freeze({
  enabled: true,
  regularPrice: 430000,
  launchPrice: 365500,
  discount: 15,
  maxCustomers: 30,
  durationDays: 30
});
